import { StyleSheet } from 'react-native'

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  errorText: {
    color: '#FF3D71',
    fontWeight: 'bold',
    // marginBottom: 10,
    textAlign: 'center',
  },
})
